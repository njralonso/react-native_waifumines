import React, { useState } from 'react';
import { View, Text } from 'react-native';

export default Options = () => {

	return (
		<View style={{ 'flex': 1 }}>
			<Text>Botón de opciones</Text>
		</View >
	)
}
